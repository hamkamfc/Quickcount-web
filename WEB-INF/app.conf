app.title	=Quick Count
app.name	=QuickCount
app.extension	=.php
app.path	=/quickcount
app.module.dir	=/module
app.paging.size	=50
;
; app.content.type values:
; - 0 (single): default, one single module displayed in content area
; - 1 (tabbed): module displayed per tab
;
app.content.type=1

;
; app.menu.mode values
; - 0	: tabbed toolbar mode
; - 1	: dropdown menu mode
;
app.menu.mode	=0

;;
;; SQlite (Default)
;;
;db.url		=sqlite:/WEB-INF/db/jaring.sq3

;;
;; Posgresql
;;

;; JDBC
;db.class	=org.postgresql.Driver
;db.url		=jdbc:postgresql://localhost/jaring

;;
;; Mysql
;;

;; JDBC
;db.url		=jdbc:mysql://127.0.0.1/jaring

;; PHP::PDO
db.class	=
db.url		='mysql:host=localhost;dbname=jaring'

db.username	=jaring
db.password	=x10cjaring2013
db.pool.min	=10
db.pool.max	=100
